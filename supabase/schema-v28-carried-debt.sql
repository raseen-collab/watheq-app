-- ============================================================
-- وثيق — schema-v28: الدين المرحَّل
--
-- ثغرتان تُسقطان مالًا بصمت، ظهرتا في محاكاة أربع سنوات:
--
-- 1) التجديد: renewContract يصفّر paid_periods و partial_amount. فإن جُدّد
--    عقد على مستأجر عليه متأخرات، اختفت متأخراته من النظام تمامًا — ولا
--    أثر في أي تقرير. مع 897 تجديدًا في أربع سنوات، هذا نزيف مستمر.
--
-- 2) التأجير الجديد: الوحدة المُخلاة يُعاد استعمال صفّها للمستأجر الجديد،
--    فيختفي دين المستأجر السابق (رأينا 78 ألف ريال تتلاشى إلى 33 ألفًا).
--
-- الحل: عمود يحمل الدين المرحَّل. لا يُطالَب به كإيجار قادم، لكنه يبقى
-- ظاهرًا في اللوحة والكشوف حتى يُسدَّد أو يُشطب بقرار صريح.
-- ============================================================
alter table tenants add column if not exists carried_debt numeric not null default 0
  check (carried_debt >= 0);
alter table tenants add column if not exists carried_debt_note text;

comment on column tenants.carried_debt is
  'دين مرحَّل من عقد سابق أو من مستأجر سابق — يظهر في الكشوف ولا يدخل في احتساب دفعات العقد الجاري';
